# write a program to accept length and width from user and calculate and display area
length = input("Enter length")
length = int(length)

width = input("Enter width")
width = int(width)

area = length * width 
print(f"area = {area}")
